(stdin => {
  // Define Function 関数定義


  // Declare Variable 変数定義
  const inputs  = stdin.toString().trim().split('\n');

  // Main Procedure メイン処理
  const result = ((tsn) => {


  })(inputs);

  // Display 描画処理
  console.log(result);

})(require('fs').readFileSync('/dev/stdin', 'utf8'))
